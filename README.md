# trae-work-checkin-plus

一个 Trae 自定义 **Skill**：本机自动领取 Trae Work（TraeCode/CN 客户端）每日签到积分，并可只读查询当前积分余额。

> 保留了 `SKILL.md`、`lib.js`、`balance.js`、`checkin.js`、`run-checkin.sh` 的原始结构与命名，方便用 TRAE 的「手动导入外部技能」方式整体导入。

## 功能

- **每日签到**：`node scripts/checkin.js` —— 幂等；今日已签则跳过，未签则调用官方领取接口签到（基础 + 额外奖励）。
- **余额查询**：`node scripts/balance.js`（`--json` 输出原始数据）—— 只读，不修改任何数据。
- **自动读本地登录态**：读客户端 `storage.json` 中的 `iCubeAuthInfo://icube.cloudide`（`tc` 加密格式）取令牌，临期自动用 refreshToken 换新，无需重复登录。
- **系统级定时**：`scripts/run-checkin.sh` 是 cron / launchd 的包装脚本，运行不依赖客户端是否打开。

## 安装（作为 Skill 导入）

1. 下载本仓库打包的 zip 或直接拉取目录。
2. 在 TraeCode 的「技能」面板选择 **手动导入外部技能**，上传 `SKILL.md`（或含 `SKILL.md` 的 zip）。
3. 确认自动填充的名称 / 描述 / 指令后确定。

## 用法

```bash
node scripts/checkin.js    # 签到
node scripts/balance.js    # 查积分

# 系统级每日定时(每天09:00)
crontab:
0 9 * * * /path/to/run-checkin.sh
# 或 macOS launchd（见 scripts/run-checkin.sh 注释）
```

## 安全说明

- 令牌只在脚本内存中，**绝不打印 / 落盘 / 上传**。
- 只对本机已登录的本人账号操作；**禁止批量账号 / 刷奖 / 绕过验证**。
- 登录态解密与所用接口均为逆向自官方客户端的非公开实现，可能随客户端版本变化（失效时需同步更新）。

## 免责声明

本项目仅供学习与个人自动化使用。请遵守 TRAE 平台规则，仅用于自己有账号的机器。若你使用本项目产生的任何违规后果，由使用者自担。

## License

### 协议说明

本项目使用 [MIT License](./LICENSE) 开源。

MIT 是一种宽松型开源协议，授权你（在此处使用即表示你接受该协议）在保留版权声明与许可声明的前提下，可以：

- **使用**：运行本项目，包括在你自己的设备上进行每日签到与余额查询；
- **复制与分发**：复制、转发本项目的源码或二进制/打包产物；
- **修改**：fork 并修改以满足你的需求；
- **再分发**：将修改后的版本再行分发（含闭源／商业再分发，但需**保留原始版权与许可声明**，并注明改动）；
- **私人使用**：不限场景与数量地用于个人自动化。

### 关键条款（MIT 的要点）

1. **附带声明**：再分发时必须在源码或文档中附带本协议与原版权声明；
2. **不提供担保（AS-IS）**：本项目按现状提供，**不附带任何明示或默示的担保**，包括但不限于适销性、特定用途适用性与不侵权；
3. **责任豁免**：因使用本项目而产生的一切责任、损失或索赔（包括间接损失），作者与贡献者**概不负责**；
4. **仅限软件本身**：MIT 授予的是对本软件代码的权利，**不授予**对 Trae / TraeWork 账号、积分、服务或 API 的任何权利。

### 免责与边界

- 本项目仅供**学习与个人自动化**使用，请遵守 [TRAE 平台规则](https://www.trae.cn/)。
- 仅应在**你自己拥有账号**的机器上使用；**禁止**批量注册/批量操作账号、刷奖、绕过验证或任何形式的滥用。
- 本项目中登录态解密与所用接口，均为**逆向自官方客户端的非公开实现**，可能随客户端版本变化而在未来失效；作者不保证其持续可用，也无法对其在官方侧产生的后果负责。
- 本协议仅约束本仓库代码，**与 Trae 官方服务条款、隐私政策无关**。若本项目与你所处地区的法律法规或你所用平台的服务条款存在冲突，由**使用者自行评估并承担相应责任**。

### 第三方与内容声明

- 项目未内置任何第三方付费组件；其运行依赖你的本机 Node.js 与 Trae 客户端登录态。
- 项目内含的 API 路径、密钥存储键名等标识，仅用于与本机客户端对接，不包含任何账号凭据。

### 完整文本

协议的完整法律文本见 [LICENSE](./LICENSE)。

---

_注意：以上为协议要点的人性化说明，不构成法律意见；一切以 `LICENSE` 中的正式文本为准。_